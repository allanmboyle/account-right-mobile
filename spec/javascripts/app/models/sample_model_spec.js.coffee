define(["app/models/contact"], (Contact) ->

  describe("An AMD model", () ->
    it("should be successfully loaded", () ->
      expect(true).toBe(true)
    )
  )

  describe("A test verifying the configuration of Jasmine", () ->
    it("should successfully execute", () ->
      expect(true).toBe(true)
    )
  )

)
